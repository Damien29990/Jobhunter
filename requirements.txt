# Talent Scout Agent — Hong Kong IT headhunting
# Python 3.11+ recommended
# Install: pip install -r requirements.txt
#
# Windows: sqlite-vec loads a native SQLite extension. Python's bundled
# SQLite usually allows this; if load_extension fails, use a SQLite build
# compiled with SQLITE_ENABLE_LOAD_EXTENSION.
#
# API keys (do not commit): set TAVILY_API_KEY and DEEPSEEK_API_KEY in a .env file.
# LLM calls go to https://api.deepseek.com via the OpenAI Python SDK.

openai>=1.54.0,<3
pydantic>=2.9.0,<3
tavily-python>=0.5.0
httpx>=0.27.0
sqlite-vec>=0.1.6
python-dotenv>=1.0.1
tzdata>=2024.1
json-repair>=0.30.0
langgraph>=0.2.0
langchain-core>=0.3.0
apscheduler>=3.10.0
pypdf>=5.0.0
